# LAB1 — Task Tracker (no-style)

This branch preserves the **original HTML-only version** of the Task Tracker page, before any CSS styling or JavaScript interactivity was added.

For the full, styled, and interactive version, see the `main` branch.

## Files

| File | Description |
|------|-------------|
| `index.html` | Page structure only — no CSS or JS linked |